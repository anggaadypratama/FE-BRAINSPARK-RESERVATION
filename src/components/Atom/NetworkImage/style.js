import {makeStyles} from "@material-ui/core/styles";

const NetworkImageStyle = makeStyles(() => ({
	image: {
		width: "100%",
		height: "auto",
	},
	imageWrapper: {
		borderRadius: 20,
	},
}));

export default NetworkImageStyle;
